<div class="content_header">
  <h4  id="align_content">Resultado da pesquisa</h4>
  <h6>
   <span >Clique sobre um dos itens</span>
  </h6>

  </h6> 
</div>
<%if @results.empty? || (@results[:company].empty? && 
	 @results[:agency].empty? && @results[:program].empty?) %>
    <h1> Não encontrou nenhum resultado</h1>
<%else %>
    <%= render 'list_agencies' %>
    <%= render 'list_companies' %>
    <%= render 'list_programs' %>
<% end %>
<div id="margin" style="margin-bottom:150px;"></div>
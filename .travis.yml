language: ruby

before_script:
  - cp config/database.yml.travis config/database.yml
  - mysql -e "create database IF NOT EXISTS aonde_test;" -uroot
  - rake db:migrate RAILS_ENV=test

addons:
  code_climate:
    repo_token: 5628bc3ae30ba0384803123a

script: rake test

rvm:
  - 2.2.0
notifications:
  email:
    recipients:
      - jonathan.rufinotkd@gmail.com
branches:
  only:

  - master
  - develop
